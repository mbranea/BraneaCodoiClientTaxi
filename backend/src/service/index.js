const user = require('./user');
const order = require('./order');
const client_account = require('./client_account');
const client_info = require('./client_info');
const registered_adresses = require('./registered_adresses');
const credit_card_info = require('./credit_card_info');
module.exports = {
  user,order,client_account,client_info,registered_adresses
};